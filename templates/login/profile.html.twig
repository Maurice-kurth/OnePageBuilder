{% extends 'base.html.twig' %}
{% block body %}
  <div class="is-hero is-small">
    <h1>Mon profil</h1>
  </div>

  <div class="container">
    <p>{{ user.username }}</p>

    {{ user.siteweb.nomSite }}
    <p>Compte créé le : X/X/X</p>
  </div>
{% endblock %}
