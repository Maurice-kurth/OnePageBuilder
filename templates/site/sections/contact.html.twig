<section id="contact" class="contact-section container is-max-desktop is-flex is-justify-content-center px-2">
  <div class="tile is-ancestor">
    <div class="tile is-12 is-parent">
      <div class="tile is-child block">
        <h3 class="title">Coordonnées</h3>
        <p class="is-flex is-align-items-center">
          <i class="fas fa-phone mr-2"></i> <a href="tel:{{ contactinfo.telephone }}">{{ contactinfo.telephone }}</a>
        </p>
        <br />
        <i class="fas fa-at mr-1"></i>
        <a href="mailto:{{ contactinfo.email }}">{{ contactinfo.email }}</a>
      </div>
      <div class="tile is-child">
        <h3 class="title">Adresse</h3>
        <address>
          {{ contactinfo.adresse }} <br /> {{ contactinfo.codePostal }} {{ contactinfo.ville }}
        </address>
      </div>
      <div class="tile is-child">
        <h3 class="title">Horaires</h3>
        <div class="horaires">{{ contactinfo.horaires }}</div>
      </div>
    </div>
    {# 
    <div class="tile is-parent">
      <div class="tile is-child">
        <h2 class="title">Contactez-nous</h2>
        <form class="contact-form" action="">
          <div class="field">
            <label class="label">Nom</label>
            <div class="control">
              <input class="input" type="text" placeholder="Votre nom" />
            </div>
          </div>
          <div class="field">
            <label class="label">Email</label>
            <div class="control">
              <input class="input" type="email" placeholder="Votre email" />
            </div>
          </div>
          <div class="field">
            <label class="label">Message</label>
            <div class="control">
              <textarea class="textarea" placeholder="Votre message"></textarea>
            </div>
          </div>
          <div class="field">
            <div class="control">
              <button class="button is-secondary">Envoyer votre message</button>
            </div>
          </div>
        </form>
      </div>
    </div>
    #}
  </div>
</section>
